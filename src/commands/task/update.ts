import { notImplemented } from "../_stub";
import type { CommandHandler } from "../../cli/types";

export const taskUpdateCommand: CommandHandler = async (_ctx, input) => {
  return notImplemented("task update", input);
};
